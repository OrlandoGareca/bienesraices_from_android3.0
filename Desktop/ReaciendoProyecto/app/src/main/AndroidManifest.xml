<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.orlandogareca.bienesraices">
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- To auto-complete the email text field in the com.orlandogareca.bienesraices.login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity"/>
        <activity android:name=".ingresar_clase" />
        <activity android:name=".pagina_principal" />
        <activity android:name=".opcion_izquierda" />
        <activity android:name=".terminos_y_condiciones" />
        <activity android:name=".vista_previa" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAYRDFAu7Q7Sx61H5vHmZi_JmHr_zNjauI" />

        <activity
            android:name=".contenedor_mi_ubicacion"
            android:label="@string/title_activity_contenedor_mi_ubicacion"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".mi_ubicacion"
            android:label="@string/title_activity_mi_ubicacion" />
        <activity android:name=".login_agente" />
        <activity android:name=".opciones_de_agentes" />
        <activity android:name=".reguistrar_nuevo" />
        <activity
            android:name=".mapas"
            android:label="@string/title_activity_mapas" />
        <activity android:name=".Login_usuarios" />
        <activity android:name=".signup" />
        <activity android:name=".contener_mapa_clientes" />
        <activity android:name=".contenedor_mapa_colegios" />
        <activity android:name=".cargar"></activity>
    </application>

</manifest>